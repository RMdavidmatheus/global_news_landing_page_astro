---
import { Button, Link } from "@heroui/react";
import { motion } from "framer-motion";
---

<motion.div
  className="w-full min-h-[500px] md:min-h-[60vh] lg:min-h-[75vh] flex flex-col md:flex-row items-center justify-center bg-gradient-to-br from-blue-900 to-blue-600 text-white px-0 py-8"
  initial={{ opacity: 0, y: -50 }}
  animate={{ opacity: 1, y: 0 }}
  transition={{ duration: 0.8 }}
  client:visible
>
  {/* Columna izquierda: texto y botones */}
  <div class="flex flex-col justify-center items-center md:items-start w-full md:w-1/2 h-full px-4">
    <h1 class="text-2xl sm:text-3xl md:text-4xl lg:text-5xl font-bold leading-tight mb-4 text-center md:text-left">
      Inteligencia de medios <span class="text-orange-400">en tiempo real</span>
    </h1>
    <p class="text-sm sm:text-base md:text-lg mb-6 font-thin text-center md:text-left">
      Estrategias basadas en datos para decisiones más inteligentes en
      sectores públicos y privados.
    </p>
    <div class="flex flex-col sm:flex-row justify-center md:justify-start items-center gap-4 mt-2 w-full">
      <a href="#contact" class="w-full sm:w-auto">
        <Button
          radius="md"
          size="lg"
          className="text-white py-3 px-6 font-bold text-base w-full sm:w-auto"
          color="secondary"
          variant="solid"
          client:visible
        >
          Solicita tú informe gratuito
        </Button>
      </a>
      <Link href="#contact" className="w-full sm:w-auto">
        <Button
          radius="md"
          size="lg"
          color="default"
          variant="bordered"
          className="text-white mb-1 font-bold w-full sm:w-auto"
          client:visible
        >
          Contáctanos
        </Button>
      </Link>
    </div>
  </div>
  {/* Columna derecha: imagen SOLO EN DESKTOP */}
  <div class="hidden md:flex justify-center items-center w-1/2 h-full">
    <img
      src="/images/monitoreo_imagen.png"
      alt="Ilustración de monitoreo de medios"
      class="max-w-full max-h-[35vh] md:max-h-[50vh] object-contain"
      loading="eager"
    />
  </div>
</motion.div>
